# Copy this file to .env (never commit .env)

# IP address of Board 1 (COM10) — printed on serial at boot after connecting to WiFi
# Example: ESP_IP=192.168.1.42
ESP_IP=

# Web server port (default 3000)
PORT=3000
