{
  "name": "esp32-color-server",
  "version": "1.0.0",
  "description": "Color picker web server — calls ESP32 HTTP API, which forwards color via ESP-NOW",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
